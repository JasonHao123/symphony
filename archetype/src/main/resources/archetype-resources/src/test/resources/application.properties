spring.${scheme}.datasource.url=jdbc:hsqldb:mem:${scheme}
spring.${scheme}.datasource.username=sa
spring.${scheme}.datasource.password=anypassword
spring.${scheme}.datasource.type=hsqldb
spring.${scheme}.datasource.resourceName=${scheme}DS
spring.${scheme}.datasource.persistenceUnitName=${scheme}PU
spring.${scheme}.datasource.hibernateProperties=${scheme}-hibernate.properties

